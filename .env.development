# Development: use the bundled fixture so the dev server works offline and
# without depending on a deployed matches.json. Switch to `remote` to test
# the real fallback chain locally.
VITE_DATA_SOURCE=remote
